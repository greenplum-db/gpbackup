platform: linux

image_resource:
  type: docker-image

inputs:
- name: gpbackup_tar
- name: gpdb_src
- name: bin_gpdb
- name: gpbackup
- name: gpbackup-tools-versions

outputs:
- name: gppkgs

params:
  OS:

run:
  path: gpbackup/ci/scripts/build-gppkgs.bash
